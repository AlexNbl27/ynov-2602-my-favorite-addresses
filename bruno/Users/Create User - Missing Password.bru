meta {
  name: Create User - Missing Password
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/users
  body: json
  auth: none
}

body:json {
  {
    "email": "{{userEmail}}"
  }
}

tests {
  test("should return 400 when password is missing", function() {
    const data = res.getBody();
    expect(res.getStatus()).to.equal(400);
    expect(data).to.have.property("message");
  });
}
