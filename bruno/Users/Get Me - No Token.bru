meta {
  name: Get Me - No Token
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/users/me
  body: none
  auth: none
}

tests {
  test("should return 403 when no token is provided", function() {
    expect(res.getStatus()).to.equal(403);
  });
}
